在 Git 提交信息里，常见的前缀通常遵循 **Conventional Commits** 规范

| 前缀         | 含义                             | 示例                              |
| ------------ | -------------------------------- | --------------------------------- |
| **feat**     | 新功能                           | `feat: 添加用户登录功能`          |
| **fix**      | 修复 bug                         | `fix: 修复登录时的空指针异常`     |
| **docs**     | 文档修改                         | `docs: 更新 README 使用说明`      |
| **style**    | 代码格式调整（不影响逻辑）       | `style: 调整缩进和空格`           |
| **refactor** | 代码重构（非新增功能或修复 bug） | `refactor: 优化数据处理逻辑`      |
| **perf**     | 性能优化                         | `perf: 提升查询速度`              |
| **test**     | 增加或修改测试用例               | `test: 添加单元测试覆盖率`        |
| **chore**    | 构建过程或辅助工具的变动         | `chore: 更新依赖版本`             |
| **ci**       | 持续集成相关配置                 | `ci: 修改 GitHub Actions 配置`    |
| **build**    | 构建系统或外部依赖的变动         | `build: 升级 webpack 到 v5`       |
| **revert**   | 回滚某次提交                     | `revert: 回滚 feat: 添加登录功能` |

## 连接本地 Linux 虚拟机与云端 GitHub 

我们可以将这一连串操作划分为三个核心阶段：

### 第一阶段：本地仓库建设 (Local Setup)

*目标：把你的文件夹变成一个受 Git 管理的“仓库”。*

1. **`git init`**
   - **动作**：在文件夹里新建了一个隐藏的 `.git` 目录。
   - **意义**：从这一刻起，这个文件夹不再是普通的文件夹，而是一个能记录历史的仓库。
2. **`git config ...(--global user.name "Shrymp-Y5S1"/.email "你的GitHub注册邮箱")`**
   - **动作**：告诉 Git 你是谁（名字和邮箱）。
   - **意义**：以后的每一次代码提交，都会盖上你的“署名印章”。
3. **创建 `.gitignore` 和 `.editorconfig`**
   - **动作**：定义规则。
   - **意义**：
     - `.gitignore` 拦住了 VCS 产生的垃圾文件（simv, csrc），只放行源码。
     - `.editorconfig` 锁定了 LF 换行符，保证跨平台兼容。
4. **`git add .`**
   - **动作**：把文件放入“暂存区”。
5. **`git commit -m "..."`**
   - **动作**：正式“归档”。
   - **关键点**：这一步解决了你遇到的 `branch rename failed` 报错。因为必须先有第一次提交，分支（master）才会真正存在。
6. **`git branch -M main`**
   - **动作**：把默认分支改名为 `main`，顺应现在的 GitHub 规范。

------

### 第二阶段：安全握手协议 (SSH Authentication)

*目标：让 GitHub 认识并信任你的 Linux 虚拟机，免去每次输密码的烦恼。*

1. **`ssh-keygen -t ed25519 ...(-C "zefengy89@gmail.com")`**
   - **动作**：在 Linux 里生成了一对钥匙（公钥 `id_ed25519.pub` 和 私钥 `id_ed25519`）。
2. **GitHub 网页添加 SSH Key**
   - **动作**：你把“公钥”复制粘贴到了 GitHub 网站上。
   - **意义**：相当于把你的“锁芯”装到了 GitHub 的大门上。
3. **`ssh -T git@github.com`**
   - **动作**：测试连接。
   - **关键点**：你输入了 `yes` 并看到了 `Hi Shrymp-Y5S1!`，证明握手成功。

------

### 第三阶段：云端同步 (Remote Sync)

*目标：建立本地与云端的永久连接。*

1. **`git remote add origin ...(git@github.com:Shrymp-Y5S1/ChipVerify-Practice.git)`**
   - **动作**：给你的本地仓库存了一个“联系人”，名字叫 `origin`，地址是你的 GitHub 仓库地址。
2. **`git push -u origin main`**
   - **动作**：把本地 `main` 分支的代码推送到云端 `origin`。
   - **结果**：看到了 `Writing objects: 100%`，并在网页上看到了代码。

------

### 一张图总结你的数据流

1. **Working Dir (Z:盘/Linux)**：你写代码的地方。
2. **Staging Area**：`git add` 后的暂存区。
3. **Local Repo**：`git commit` 后的本地历史库。
4. **Remote Repo (GitHub)**：`git push` 后的云端备份。

---

## git提交规范

> [!tip]
>
> ### Git 的作用范围
>
> - **Git 的管理单位是仓库 (repository)**，而不是单个文件夹。
> - 一个仓库的根目录下会有一个隐藏的 `.git` 文件夹，它记录了整个项目的版本信息。
> - 无论你在仓库的哪个子目录里执行 `git` 命令，Git 都会 **向上查找最近的** `.git` **文件夹**，然后以那个目录作为仓库根来操作。

### ⚠️ 提交前的“生死三项” (注意事项)

在敲下任何 `git` 命令前，请务必在 Linux 终端执行以下操作：

1. **执行 `make clean` (核心)**：
   - **原因**：VCS 产生的 `csrc`、`simv` 和数十 MB 的 `fsdb` 属于垃圾文件。
   - **后果**：如果不清理就提交，你的 GitHub 仓库很快会变得臃肿且难以维护。
2. **检查目录结构**：
   - 确保你的 `.v` 文件都在 `rtl/` 文件夹，`.v` (TB) 文件都在 `tb/` 文件夹。
   - 检查文件名是否规范（例如：`priority_mux.v` 而不是 `111.v`）。
3. **自查 `.gitignore`**：
   - 再次确认 `.gitignore` 是否生效（输入 `git status`，如果看不到那些编译产生的临时文件夹，说明生效了）。

------

### 🚀 规范化 Git 通用提交流程

请按顺序执行以下指令。这个流程适用于你未来所有的项目开发：

| **步骤**    | **指令**                                                     | **目的**                                 |
| ----------- | ------------------------------------------------------------ | ---------------------------------------- |
| **1. 清理** | `make clean`                                                 | 抹除所有仿真痕迹，只留源码               |
| **2. 查看** | `git status`                                                 | 确认哪些文件被修改/新增，防止误删或漏加  |
| **3. 暂存** | `git add .`                                                  | 将**“当前目录”**所有更改标记为“准备提交” |
| **4. 存档** | `git commit -m "feat: complete combinational logic modules"` | **本地归档**，并附带简练的备注           |
| **5. 同步** | `git push origin main`                                       | **推送到云端**，完成最终备份             |

> [!tip]
>
> 除了使用`git add .`
>
> 如果想在子目录里把整个仓库的变化都抓进来，有两个选择：
>
> - **方法 A**：使用 `-A` 参数（All）。
>
>   ```Bash
>   git add -A :/
>   ```
>
>   **符号解析**：`:/` 在 Git 命令中代表 **“仓库根目录”**。
>
>   这个命令的意思是：**“从仓库根目录开始，把所有变化（新增、修改、删除）全部加入暂存区。”**
>
> - **方法 B**：回到根目录执行。
>
>   ```Bash
>   cd ..
>   git add .
>   ```

------

### 总结

如果你现在就在 `RTL_Practice` 目录下，直接复制这四行：

```Bash
make clean
git status
git add .
# git reset（恢复到未 add 状态，文件仍然保持修改状态）
# git add -A (存在文件删除请用这个)
# git add -u . (仅当前目录下已追踪的修改文件，不深入子目录)
# git add --all
git commit -m "feat: add Mux, Full-Adder and Priority-Mux modules"
git push origin main
```

> 小贴士：
>
> 提交消息建议使用前缀，如 feat: (新功能), fix: (修 Bug), docs: (写报告)。这会让你在周日的汇报中展示 Git 记录时，显得极其专业。

## 丢弃修改 / 保留修改但不提交

```shell
# 丢弃修改
git checkout -- Notes/HVL_SV.md
```

```shell
# 把它加入 .gitignore 或者用 git update-index 标记为不跟踪修改
git update-index --assume-unchanged Notes/HVL_SV.md
# 取消标记
git update-index --no-assume-unchanged Notes/HVL_SV.md
```

## 删除

在 Linux 终端里删除当前目录下的 `gen_tree.py` 文件，可以直接用 **`rm` 命令**：

```bash
rm gen_tree.py
```

> [!tip]
>
> - 如果你想在删除前确认文件存在，可以先用：
>
>   ```shell
>   ls -l gen_tree.py
>   ```
>
> - 如果想避免误删，可以加上交互确认参数：
>
>   ```shell
>   rm -i gen_tree.py
>   # 系统会提示你是否真的要删除。
>   ```
>
> - 如果文件有写保护，可能需要：强制删除。  
>
>   ```shell
>   rm -f gen_tree.py
>   ```

## committed but not pushed

```shell
git commit --amend -m "新的 commit 信息"
```

